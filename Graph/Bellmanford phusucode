//Bellmanford Algorithm

Bellmanford(G,V,E,S,W)
For each vertex V (- G
dist[v] = INF
dist[s] = 0

for(i=1 to v-1)
For each edge (u,v) (- G
Relax(u,v,w)
For each edge (u,v) (- G
if(dist[u] + w[u,v] < dist[v]
return neg cycle
return dist
